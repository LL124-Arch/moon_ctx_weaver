# 0.1.0

这是第一个可运行的发布边界。它包含依赖图校验、必选闭包、预算内的稳定选择、多版本升级、上下文渲染和逐节点审计，同时提供 native JSON CLI 与三套离线场景。

由于当前版本只接受调用方提供的 token 成本和质量分，所以结果可以离线复现，也不会让一个隐藏 tokenizer 或模型调用改变规划。词法评分只是零依赖的默认信号，真实系统仍然可以传入自己的优先级。

这一版本明确不包含 Agent loop、自动摘要、向量检索、厂商适配器或精确 tokenizer。后续兼容层会建立在现有规划核心之上，而不会把这些职责塞回核心库。
